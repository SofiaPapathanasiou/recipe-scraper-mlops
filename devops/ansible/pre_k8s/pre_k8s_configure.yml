---
- name: Prepare nodes for Kubespray
  hosts: all
  become: yes
  tasks:
    - name: Stop and disable firewalld when present
      ansible.builtin.systemd:
        name: firewalld
        state: stopped
        enabled: no
        masked: yes
      failed_when: false
